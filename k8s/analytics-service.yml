apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: optisight
  labels:
    app: analytics-service
    tier: backend
    app.kubernetes.io/part-of: optisight-ai
spec:
  type: ClusterIP
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
      name: http
  selector:
    app: analytics-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: optisight
  labels:
    app: analytics-service
    tier: backend
    app.kubernetes.io/part-of: optisight-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
        tier: backend
    spec:
      containers:
        - name: analytics-service
          image: optisight/analytics-service:latest
          ports:
            - containerPort: 3003
              name: http
          envFrom:
            - configMapRef:
                name: optisight-config
            - secretRef:
                name: optisight-secrets
          env:
            - name: SERVICE_NAME
              value: analytics_service
            - name: ANALYTICS_SERVICE_PORT
              value: "3003"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: analytics-service-hpa
  namespace: optisight
  labels:
    app: analytics-service
    tier: backend
    app.kubernetes.io/part-of: optisight-ai
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: analytics-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
