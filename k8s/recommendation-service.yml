apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: optisight
  labels:
    app: recommendation-service
    tier: backend
    app.kubernetes.io/part-of: optisight-ai
spec:
  type: ClusterIP
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
      name: http
  selector:
    app: recommendation-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service
  namespace: optisight
  labels:
    app: recommendation-service
    tier: backend
    app.kubernetes.io/part-of: optisight-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: recommendation-service
  template:
    metadata:
      labels:
        app: recommendation-service
        tier: backend
    spec:
      containers:
        - name: recommendation-service
          image: optisight/recommendation-service:latest
          ports:
            - containerPort: 3004
              name: http
          envFrom:
            - configMapRef:
                name: optisight-config
            - secretRef:
                name: optisight-secrets
          env:
            - name: SERVICE_NAME
              value: recommendation_service
            - name: RECOMMENDATION_SERVICE_PORT
              value: "3004"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: recommendation-service-hpa
  namespace: optisight
  labels:
    app: recommendation-service
    tier: backend
    app.kubernetes.io/part-of: optisight-ai
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: recommendation-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
